package inf112.core.tile;

import com.badlogic.gdx.math.Vector2;

public class LaserTile extends AbstractTile {
    public LaserTile(Vector2 coordinates, TileId tileId) {
        super(coordinates, tileId);
    }
}
